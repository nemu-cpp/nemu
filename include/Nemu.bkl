nemupath = envvar("NEMU_CPP");
import $(nemupath)/core/include/Nemu/Core.bkl;

setting NEMU_CPP_ROOT
{
    help = "Path to the Nemu installation";
    default = '$(NEMU_CPP)/nemu';
}

template Nemu : NemuCore
{
    includedirs += $(NEMU_CPP_ROOT)/include;
}
